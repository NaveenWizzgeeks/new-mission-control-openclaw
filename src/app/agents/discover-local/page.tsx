import { LocalAgentsDiscoveryPanel } from '@/components/agents/LocalAgentsDiscoveryPanel';

export const metadata = { title: 'Discover Local Agents · Mission Control' };

export default function DiscoverLocalAgentsPage() {
  return <LocalAgentsDiscoveryPanel />;
}
