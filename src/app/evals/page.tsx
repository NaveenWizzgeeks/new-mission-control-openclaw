import { EvalsDashboard } from '@/components/evals/EvalsDashboard';

export const metadata = { title: 'Evals · Mission Control' };

export default function EvalsPage() {
  return <EvalsDashboard />;
}
