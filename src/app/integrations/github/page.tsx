import { GitHubSyncDashboard } from '@/components/integrations/GitHubSyncDashboard';

export const metadata = { title: 'GitHub Sync · Mission Control' };

export default function GitHubIntegrationPage() {
  return <GitHubSyncDashboard />;
}
