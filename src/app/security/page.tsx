import { SecurityDashboard } from '@/components/security/SecurityDashboard';

export const metadata = { title: 'Security · Mission Control' };

export default function SecurityPage() {
  return <SecurityDashboard />;
}
