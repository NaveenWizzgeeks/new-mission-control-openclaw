import { MarketplaceGrid } from '@/components/skills/MarketplaceGrid';

export const metadata = { title: 'Skills · Mission Control' };

export default function SkillsPage() {
  return <MarketplaceGrid />;
}
